ext {
    projectVersion = '0.1.0'
}

buildscript {
    repositories {
        mavenCentral()
    }
}

subprojects {
    buildDir = rootProject.file("build/${it.name}")

    repositories {
        mavenCentral()
    }
}

wrapper {
    gradleVersion = "6.5.1"
}